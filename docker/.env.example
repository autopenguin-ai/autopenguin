############
# AutoPenguin Self-Hosted Configuration
# Copy to .env and edit, or let setup.sh generate it
############

# ===========================================
# APP SETTINGS
# ===========================================
APP_URL=http://localhost:3000
EMAIL_FROM_DOMAIN=localhost

# ===========================================
# SUPABASE CORE (auto-generated by setup.sh)
# ===========================================
POSTGRES_PASSWORD=your-super-secret-postgres-password
JWT_SECRET=your-super-secret-jwt-token-at-least-32-chars
ANON_KEY=your-anon-key
SERVICE_ROLE_KEY=your-service-role-key
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=admin

# Supabase internal (don't change unless you know what you're doing)
SUPABASE_PUBLIC_URL=http://localhost:8000
POSTGRES_DB=postgres
POSTGRES_PORT=5432

# ===========================================
# FRONTEND (Vite build-time)
# ===========================================
VITE_SUPABASE_URL=http://localhost:8000
VITE_SUPABASE_ANON_KEY=${ANON_KEY}

# ===========================================
# LLM (default: bundled Ollama)
# ===========================================
# Ollama is included in Docker Compose.
# To use a cloud provider, uncomment and set:
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# OPENROUTER_API_KEY=sk-or-...
OLLAMA_HOST=http://ollama:11434
DEFAULT_LLM_PROVIDER=ollama
DEFAULT_LLM_MODEL=llama3.2:3b

# ===========================================
# EMAIL (optional — skip for console-only auth)
# ===========================================
# RESEND_API_KEY=re_...

# ===========================================
# N8N (optional)
# ===========================================
# N8N_WEBHOOK_SECRET=your-n8n-webhook-secret

# ===========================================
# AUTH
# ===========================================
GOTRUE_SITE_URL=${APP_URL}
GOTRUE_EXTERNAL_EMAIL_ENABLED=true
GOTRUE_MAILER_AUTOCONFIRM=true
GOTRUE_SMS_AUTOCONFIRM=true
GOTRUE_DISABLE_SIGNUP=false
GOTRUE_JWT_EXPIRY=3600
GOTRUE_JWT_DEFAULT_GROUP_NAME=authenticated

# ===========================================
# STUDIO (Supabase admin dashboard)
# ===========================================
STUDIO_PORT=8443
STUDIO_DEFAULT_ORGANIZATION=AutoPenguin
STUDIO_DEFAULT_PROJECT=Self-Hosted
